/** Stub: spätere Übergänge zu Penalty/Freistoß/Keeper-Challenge. */
export function triggerMinigame(type, payload) {
  return { triggered: false, type, payload, note: 'Stub only in V1 sandbox.' };
}

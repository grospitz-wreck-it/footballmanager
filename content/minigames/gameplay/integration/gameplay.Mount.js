/**
 * Stub API: zukünftiger Einstiegspunkt zur Main-App-Integration.
 */
export function mountGameplaySystem(options = {}) {
  return {
    mounted: false,
    mode: 'sandbox',
    options,
    note: 'Stub only. No live integration in V1.'
  };
}
